<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% stylesheets
            'assets/css/fop.css'
            'vendor/fontawesome/css/font-awesome.min.css'
            'vendor/c3js-chart/c3.min.css' filter='cssrewrite' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>

        <nav id="site-navbar" class="navbar navbar-default navbar-static-top">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#site-navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand bg-alizarin" href="{{ path('homepage') }}">
                        <img src="{{ asset('logo.svg') }}" height="100%"/>
                    </a>
                </div>
                <div class="collapse navbar-collapse" id="site-navbar-collapse">
                    {{ knp_menu_render('main', { 'style': 'navbar' }) }}
                    {{ knp_menu_render('user', { 'style': 'navbar-right' }) }}
                </div>
            </div>
        </nav>
        <nav class="navbar navbar-inverse navbar-static-top">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#site-navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <div class="navbar-brand">Hi Brieuc</div>
                </div>
                <div class="collapse navbar-collapse" id="site-navbar-collapse">
                    {{ knp_menu_render('locale_switcher', { 'style': 'navbar-right', 'currentClass': '', 'template': ':menu:locale_switcher.html.twig' }) }}
                </div>
            </div>
        </nav>

        <div id="site-container">
            {% block content %}{% endblock %}
        </div>


        <footer id="site-footer">
            <div class="container">
                {{ knp_menu_render('footer', { 'style': 'site-footer' }) }}
                <p class="text-muted">&copy; {{ 'now'|date('Y') }} {{ 'footer.copyright'|trans }}</p>
            </div>
        </footer>


        {% block requirejs include("requirejs.html.twig") %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
