<script src="{{ asset('vendor/requirejs/require.js') }}"></script>
<script>
    requirejs.config({
        baseUrl: '{{ asset('assets/js') }}',
        paths: {
            jquery: '{{ asset('vendor/jquery/dist/jquery.min') }}',
            bootstrap: '{{ asset('vendor/bootstrap/dist/js/bootstrap.min') }}',
            countdown: '{{ asset('vendor/jquery.countdown/dist/jquery.countdown.min') }}'
        },
        shim: {
            bootstrap: ['jquery'],
            countdown: ['jquery']
        }
    });
    require({{ ['common']|merge([module|default])|json_encode|raw }});
</script>
