{% extends 'base.html.twig' %}

{% set module = 'app/seasons/graphs' %}

{% block content %}
    <div class="section section-inverse">
        <div class="container">
            <h1>{{ 'season.title'|trans({ '%year%': season.year }) }}</h1>
        </div>
    </div>

    <div class="container">
        {{ knp_menu_render('season', { 'style': 'tabs' }) }}
    </div>
{% endblock %}
