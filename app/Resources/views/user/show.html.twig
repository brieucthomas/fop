{% extends 'base.html.twig' %}

{% block title user.username %}

{% block body %}
    <div class="container">
        <h1>{{ user.username }}</h1>
        <img src="{{ gravatar(user.email) }}" />
    </div>
{% endblock %}
